# 特性自动化迁移预审报告

## 2026-08-08 检查点（意念守护）

当前审计为 `full 235 / partial 188 / dm_only 76`。本 Goal 从 `227/195/77` 开始，已完成预兆池、信实坐骑、高阶防守战术、钢铁意志、专业预言、战争祭司和意念守护的真实端到端闭环；领域 helper、配置覆盖数和单一分支不计入 full。

### 当前新增真实消费者

- `钢铁意志`：升级选择按已有感知豁免熟练执行替换规则，角色熟练、战斗快照 modifier 和实际豁免入口共享同一选择。
- `专业预言`：施法预览/确认校验预言学派、二环以上、普通法术位消耗和恢复槽位输入；槽位 CAS、幂等交易和仪式/免费施法拒绝均在 Spell Economy 内完成。
- `高阶防守战术`：伤害前 reaction window、单一伤害类型抗性 effect、目标回合结束生命周期和后续伤害防御解析已接通；混合伤害不猜测。
- `战争祭司`：附赠动作要求玩家提交一个已配置的近战武器攻击或徒手打击名；玩家攻击服务复用该攻击的骰伤/目标/射程规则，同时保留特性自身的 5 尺敌对目标、动作经济和感知调整值资源。资源由 CombatEngine 的 CAS/幂等链消费，玩家房间不会重复扣除。
- `意念守护`：`damage_resistance` 由共享防御解析器实际应用；`guarded_mind_clear` 只在 `turn_start` 窗口接受玩家选择的 `charmed`/`frightened`，通过角色的 `psionic_dice:战士` 资源扣除 1 枚并清除状态/效果，重复请求返回同一 CombatAction。

### 预置 D20 池审计边界

本轮逐条复核 `roll_intervention` 预审候选：46 条中 25 条已经 full、21 条仍为
`consumer_partial`。严格同时满足“长休生成并持久化多枚 D20、检定前替换、属性/技能/豁免/攻击全入口、每回合限制、跨目标可见性与距离、长休清理、玩家/DM 输入、CAS/幂等消费”的只有法师预言师「预兆」及其升级「高等预兆」两条审计行（同一底层池，数量由 2 改为 3）。

「混乱之潮」是自身一次性检定优势且还带法术位恢复后的狂野魔法浪涌；「归复平衡」是目标优势/劣势抵消；「现世传说」「专心炽志」是失败后重骰；其余候选还包含攻击、状态、光环、传送、召唤或多分支效果。它们不能与预置 D20 池共享完整语义，因此不凑成 25 条批次，也没有因为 helper 或配置而改成 `full`。证据产物：`/private/tmp/audit-live.json`、`/private/tmp/plan-live.json`。

对 499 条源码描述再做了未依赖审计关键词的语义扫描，直接命中“检定前/掷出 d20 前/预置骰/替换骰”的只有 4 条：宇宙预兆、序列意识、混乱之潮、预兆；前 3 条分别是奇偶加减骰、持续最低值和自身单次优势，不是预置池替换。这个扫描排除了审计脚本漏检导致候选数量不足的可能。

### 2026-08-08 长执行安全边界复核

本轮在预置 D20 池审计之外，对资源恢复、状态生命周期、动作触发、光环范围和成长授予候选做了第二轮语义级反审计。资源簇的非 full 候选虽然有 24 条，但分别依赖免费施法、资源转换、持续状态、目标选择和复杂分支；其他簇同样不是可互换的完整合同。独立单积木候选的最大严格上界为成长授予 13 条，不能组成用户要求的真实 `+25 full` 批次。证据保存在 `/private/tmp/semantic-cluster-audit.json`；本轮没有写入 D20 helper、没有改变审计状态，也没有把候选覆盖数计为 full。

这份报告只规划迁移，不修改运行时状态，也不把候选行直接升级为 `full`。

- 总条目：499
- 当前状态：`{'full': 235, 'dm_only': 76, 'partial': 188}`
- 预审状态：`{'already_full': 227, 'missing_source': 35, 'missing_runtime_contract': 185, 'consumer_partial': 35, 'manual_boundary': 10, 'needs_contract_review': 7}`

## 模板分组

| 模板 | 条目 | 已 full | 缺运行时合同 | 待合同复核 | 消费者不完整 | 人工边界 | 缺源码 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 掷骰干预 (`roll_intervention`) | 46 | 25 | 0 | 0 | 21 | 0 | 0 |
| 状态生命周期 (`state_lifecycle`) | 34 | 17 | 16 | 1 | 0 | 0 | 0 |
| 动作经济与触发条件 (`action_trigger`) | 34 | 22 | 11 | 0 | 0 | 1 | 0 |
| 通用被动/数值修正 (`passive_modifier`) | 79 | 29 | 14 | 0 | 0 | 1 | 35 |
| 成长授予/升级选择 (`progression_grant`) | 67 | 54 | 12 | 0 | 0 | 1 | 0 |
| 资源/恢复/频率 (`resource_lifecycle`) | 64 | 32 | 24 | 2 | 0 | 6 | 0 |
| 光环/范围被动 (`aura_passive`) | 48 | 5 | 42 | 1 | 0 | 0 | 0 |
| 伤害/治疗 (`damage_healing`) | 48 | 19 | 27 | 2 | 0 | 0 | 0 |
| 施法框架/法术修改 (`spell_capability`) | 7 | 3 | 0 | 0 | 4 | 0 | 0 |
| 命中后骑手 (`attack_rider`) | 13 | 3 | 0 | 0 | 10 | 0 | 0 |
| 0 HP/死亡生命周期 (`zero_hp_intervention`) | 8 | 3 | 5 | 0 | 0 | 0 | 0 |
| 目标/范围/豁免组合 (`target_save_status`) | 10 | 3 | 6 | 0 | 0 | 1 | 0 |
| 召唤/伙伴 (`summon_lifecycle`) | 13 | 7 | 6 | 0 | 0 | 0 | 0 |
| 伤害前/防御干预 (`pre_damage_intervention`) | 13 | 2 | 10 | 1 | 0 | 0 | 0 |
| 移动/位移 (`movement`) | 15 | 3 | 12 | 0 | 0 | 0 | 0 |

## 预审结论

- `missing_runtime_contract`：源码命中积木，但还没有真实运行时合同；不能仅靠字段改成 `full`。
- `needs_contract_review`：已有部分运行时结构，但仍需逐字段核对消费者、输入、资源和幂等。
- `consumer_partial`：执行器存在，但生产接线或安全闭环未完成。
- `manual_boundary`：包含选择、DM裁定或开放叙事，不能强行无人值守。
- 只有完成真实配置、消费者、状态写入、输入链和测试后，才允许从本报告中产生 `full` 增量。

## 下一批执行门槛

下一批必须从一个模板中选择一组条目，先生成配置和定向测试，再跑499条审计。预审数字是候选分组，不是承诺的新增 `full` 数量。
