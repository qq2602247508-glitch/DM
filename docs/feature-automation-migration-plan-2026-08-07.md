# 特性自动化迁移预审报告

## 2026-08-08 检查点（真力注拳与月光飞步生命周期）

当前审计为 `full 242 / partial 182 / dm_only 75`。本 Goal 从
`227/195/77` 开始，真实净增 15；本次新增 full 是武僧「真力注拳」。月亮结社「月光飞步」此前已是 full，本次补强了其通用法术位重置、显式网格传送、回合末状态和下一次攻击优势的生产链。

### 当前新增真实消费者

- `真力注拳`：玩家攻击入口只接受结构化徒手打击和 `force/original` 明确输入；单一基础伤害段的伤害类型在最终组件结算前覆盖为力场，非法武器、多段伤害或未授予特性均拒绝。攻击结果写入真实伤害类型，并通过玩家攻击幂等键重放不重复扣 HP。
- `月光飞步`：附赠动作从角色资源消费一次；资源不足时，玩家提交二环至九环法术位之一，服务端在同一事务内通过资源 CAS 恢复并立即扣除一次特性使用；目标行列经过权威网格边界、占用和距离校验。传送后创建持久化 `moonlight_step` 回合状态，攻击上下文读取该状态并在下一次攻击消费，重复请求返回同一 CombatAction。

## 2026-08-08 检查点（自然守御）

当前审计为 `full 240 / partial 183 / dm_only 76`。本 Goal 从 `227/195/77` 开始，已真实净增 13；本次新增的自然守御不是配置覆盖数，而是长休选择、资源持久化、条件免疫和动态伤害防御的完整链。

### 自然守御真实消费者

- 长休提交 `circle_land_terrain_choice`，严格接受荒漠、极地、温带、热带四项并写入角色 `resources.circle_land_terrain.selected`。
- 伤害防御解析器读取角色持久化选择，将地形映射为火焰、寒冷、闪电或毒素抗性；角色没有合法选择时不授予抗性。
- 条件免疫解析器独立消费 `condition_immunity(poisoned)`，所以毒素免疫不会依赖伤害类型判断。
- 定向测试覆盖运行时合同、持久化选择、抗性映射和中毒免疫；代码提交为 `0510698`，交接与审计文档另行提交。

## 2026-08-08 检查点（战斗激励）

当前审计为 `full 241 / partial 182 / dm_only 76`，本 Goal 已真实净增 14。战斗激励的两个分支均已接入真实攻击消费者：授予者的运行时合同把 `defense/offense` 能力写入目标的持久化激励骰；玩家提交模式与骰值后，防御模式增加目标 AC，进攻模式增加命中后的首段伤害；服务端复用骰面范围、动作结果、版本/CAS 与幂等消费。代码提交为 `b5609f3`，与交接文档分离。

## 2026-08-08 检查点（元素亲和）

当前审计为 `full 239 / partial 184 / dm_only 76`。本 Goal 从 `227/195/77` 开始，已完成预兆池、信实坐骑、高阶防守战术、钢铁意志、专业预言、战争祭司、意念守护、凶蛮打击、邪魔体魄、光耀之魂和元素亲和的真实端到端闭环；领域 helper、配置覆盖数和单一分支不计入 full。

### 当前新增真实消费者

- `钢铁意志`：升级选择按已有感知豁免熟练执行替换规则，角色熟练、战斗快照 modifier 和实际豁免入口共享同一选择。
- `专业预言`：施法预览/确认校验预言学派、二环以上、普通法术位消耗和恢复槽位输入；槽位 CAS、幂等交易和仪式/免费施法拒绝均在 Spell Economy 内完成。
- `高阶防守战术`：伤害前 reaction window、单一伤害类型抗性 effect、目标回合结束生命周期和后续伤害防御解析已接通；混合伤害不猜测。
- `战争祭司`：附赠动作要求玩家提交一个已配置的近战武器攻击或徒手打击名；玩家攻击服务复用该攻击的骰伤/目标/射程规则，同时保留特性自身的 5 尺敌对目标、动作经济和感知调整值资源。资源由 CombatEngine 的 CAS/幂等链消费，玩家房间不会重复扣除。
- `意念守护`：`damage_resistance` 由共享防御解析器实际应用；清除魅惑/恐慌只在 `turn_start` 窗口接受玩家选择，通过角色的 `psionic_dice:战士` 资源扣除并清除状态/效果，重复请求返回同一 CombatAction。
- `凶蛮打击`：攻击入口传入实际 `attack_roll_mode`；只有玩家显式选择放弃优势且提交合法 1d10 总值时才附伤，狂暴/鲁莽/力量武器条件和每回合使用集合由服务端验证。
- `邪魔体魄`：`rest_choice` 接受短/长休玩家选择并持久化 `selected`；防御合同按角色资源读取抗性类型，不从特性名称猜测默认值。
- `光耀之魂`：攻击服务把每个目标真实伤害类型交给骑手；目标 ID、魅力调整值和每回合使用集合都由玩家输入与服务端快照校验。
- `元素亲和`：子职选择白名单写入 advancement grant，选中的 `damage_type:*` 同时进入防御与攻击骑手，攻击服务不从法术名称猜测元素类型。

### 预置 D20 池审计边界

本轮逐条复核 `roll_intervention` 预审候选：46 条中 26 条已经 full、20 条仍为
`consumer_partial`。严格同时满足“长休生成并持久化多枚 D20、检定前替换、属性/技能/豁免/攻击全入口、每回合限制、跨目标可见性与距离、长休清理、玩家/DM 输入、CAS/幂等消费”的只有法师预言师「预兆」及其升级「高等预兆」两条审计行（同一底层池，数量由 2 改为 3）。

「混乱之潮」还带法术位恢复后的狂野魔法浪涌；「归复平衡」是目标优势/劣势抵消；「现世传说」「专心炽志」是失败后重骰。其余候选还包含攻击、状态、光环、传送、召唤或多分支效果，不能与预置池共享完整语义，因此继续保持 partial。

对 499 条源码描述做了未依赖审计关键词的语义扫描，直接命中“检定前/掷出 d20 前/预置骰/替换骰”的只有少数条目；其他命中项分别是奇偶加减骰、持续最低值或自身单次优势，不是预置池替换。这排除了用关键词漏检扩大候选的可能。

### 长执行安全边界复核

资源、状态、动作触发、光环范围和成长授予候选必须逐条接入真实消费者；配置、领域 helper、兼容适配器或只落库均不计 full。任何仍依赖免费施法、资源转换、随机表、复杂召唤、未接入 UI 或开放 DM 裁定的条目继续保持 partial。最大严格同构上界不能替代逐条完整语义审计。

这份报告只规划迁移，不修改运行时状态，也不把候选行直接升级为 `full`。

- 总条目：499
- 当前状态：`{'full': 242, 'dm_only': 75, 'partial': 182}`
- 预审状态：`{'already_full': 242, 'missing_source': 35, 'missing_runtime_contract': 172, 'consumer_partial': 34, 'manual_boundary': 10, 'needs_contract_review': 6}`

## 模板分组

| 模板 | 条目 | 已 full | 缺运行时合同 | 待合同复核 | 消费者不完整 | 人工边界 | 缺源码 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 掷骰干预 (`roll_intervention`) | 46 | 26 | 0 | 0 | 20 | 0 | 0 |
| 状态生命周期 (`state_lifecycle`) | 34 | 18 | 15 | 1 | 0 | 0 | 0 |
| 动作经济与触发条件 (`action_trigger`) | 34 | 22 | 11 | 0 | 0 | 1 | 0 |
| 通用被动/数值修正 (`passive_modifier`) | 79 | 30 | 13 | 0 | 0 | 1 | 35 |
| 成长授予/升级选择 (`progression_grant`) | 67 | 54 | 12 | 0 | 0 | 1 | 0 |
| 资源/恢复/频率 (`resource_lifecycle`) | 64 | 35 | 21 | 2 | 0 | 6 | 0 |
| 光环/范围被动 (`aura_passive`) | 48 | 5 | 42 | 1 | 0 | 0 | 0 |
| 伤害/治疗 (`damage_healing`) | 48 | 26 | 21 | 1 | 0 | 0 | 0 |
| 施法框架/法术修改 (`spell_capability`) | 7 | 3 | 0 | 0 | 4 | 0 | 0 |
| 命中后骑手 (`attack_rider`) | 13 | 3 | 0 | 0 | 10 | 0 | 0 |
| 0 HP/死亡生命周期 (`zero_hp_intervention`) | 8 | 3 | 5 | 0 | 0 | 0 | 0 |
| 目标/范围/豁免组合 (`target_save_status`) | 10 | 4 | 5 | 0 | 0 | 1 | 0 |
| 召唤/伙伴 (`summon_lifecycle`) | 13 | 7 | 6 | 0 | 0 | 0 | 0 |
| 伤害前/防御干预 (`pre_damage_intervention`) | 13 | 3 | 9 | 1 | 0 | 0 | 0 |
| 移动/位移 (`movement`) | 15 | 3 | 12 | 0 | 0 | 0 | 0 |

## 预审结论

- `missing_runtime_contract`：源码命中积木，但还没有真实运行时合同；不能仅靠字段改成 `full`。
- `needs_contract_review`：已有部分运行时结构，但仍需逐字段核对消费者、输入、资源和幂等。
- `consumer_partial`：执行器存在，但生产接线或安全闭环未完成。
- `manual_boundary`：包含选择、DM裁定或开放叙事，不能强行无人值守。
- 只有完成真实配置、消费者、状态写入、输入链和测试后，才允许从本报告中产生 `full` 增量。

## 下一批执行门槛

下一批必须从一个模板中选择一组条目，先生成配置和定向测试，再跑499条审计。预审数字是候选分组，不是承诺的新增 `full` 数量。
