# Codex 交接：本地 D&D 副 DM 通用对话与叙事质量

更新时间：2026-07-31（Asia/Shanghai）

## 目标

继续完善游戏推进台中央的本地副 DM，使它不是只会生成 opener 的专用模板，而是能正确理解并交付真实跑团中不同类型的请求：

- 可直接朗读的开场、过场、成功/失败后果和场景描写；
- NPC 的单句台词或对白；
- 给 DM 的主持建议、卡团处理、破冰与引导步骤；
- 原因、动机、共同合作理由等解释或设计草案；
- 承接上一条回答的扩写、缩写、改写、换语气、换说话者和精确字数；
- 普通自由询问和后续追问。

质量目标同时包括：中文自然、有叙事节奏和画面感；允许在可朗读草案中自由发挥光影、气味、声音、物件、人物小动作和象征意象；不把草案自动写成战役事实；不替玩家决定行动、反应、感受、过去或结论；尊重玩家已作出的路线选择。

## 仓库与运行环境

- 开发仓库：`/Users/inagi/codex/700-AI/local-dnd-dm-assistant`
- 运行副本：`/Users/inagi/codex/130 游戏/135-跑团助手 dnd`
- 当前分支：`main`
- 当前基线：`f7d824d`（`fix: preserve natural table-side narration`）；本轮接手改动待提交。
- 本轮完成后，5 个功能文件与本交接文件一并提交并推送 `main`。
- 页面：`http://127.0.0.1:5173/#/game-table`
- 后端端口：`8000`
- 玩家网关端口：`8787`
- 当前本地模型：Ollama `qwen3:30b-instruct`
- 模型上下文：8,192；约 19GB；此前确认 100% GPU。
- 本轮已确认运行副本的 5173/8000/8787 与 Ollama 11434 可用；8000 使用运行副本数据库，浏览器已恢复标准团 Scene 5。

## 已完成内容

### 上一提交已经完成并推送

提交 `f7d824d` 已完成：

- 普通询问通常只调用一次模型；质量失败时最多定向重写一次。
- “我该怎么给玩家开场”识别为可朗读成品，不再默认回复主持步骤。
- “太短了”“扩写”“保持刚才风格”等可以承接上一条有效文本。
- 优质叙事候选不会仅因缺少交接问题而整段丢弃；保留正文并自然补上行动交接。
- 修正后的文本同时用于页面显示和持久对话，避免页面与会话记录分叉。
- 文案草案不进入权威事实；普通对话按团持久化，但 `authoritative=false`。

### 当前工作树已经实现但尚未提交

1. 创作参数：

- narrative 模式的 temperature 从 `0.2` 提到 `0.35`。
- 继续固定 `num_ctx=8192`，`num_predict=700`。
- 新增后端测试锁定这组参数。

2. 通用交付类型分类：

- spoken line 的识别优先于宽泛的“怎么/如何”建议。
- “店主会怎么回应？只给一句台词”识别为 `spoken_line`。
- “给我一段失败后果描写”识别为 `read_aloud`。
- 否定约束会先从分类文本中剥离，“不要 NPC 台词”不会反向触发台词模式。
- 单独出现“刚才”不再误判 revision；例如“玩家刚才调查失败”仍为 read-aloud。
- revision 覆盖太短、太长、扩写、压到 N 字、改得/改为、删掉/去掉、再来一版、保持刚才风格和上一条有误。

3. DM 建议边界：

- 默认只给主持方法，不新增 NPC 台词、异象、线索、遭遇或替代任务。
- 尊重玩家已经作出的选择，不能换一种方式把玩家强推回旧路线。
- DM 明确说“不要推进”时，禁止新增世界事件或剧情钩子。
- “不要开场旁白”时，输出元层主持步骤，不预写场景事件、NPC 扮演或玩家反应。
- 要求三条时会检查是否确实有三条可区分的方法。
- 模型连续失败后的 guidance fallback 分为“路线选择/卡团”和“玩家相识/破冰”两类。

4. NPC 台词：

- 一句台词不能带列表、旁白或明显过长。
- 撒谎场景必须让听者从台词本身感受到 NPC 已察觉谎言。
- 禁止用无关寒暄、新道具或新经历绕开说话目的。
- 撒谎场景安全回退为：“我不急着拆穿你。想清楚了，再把刚才那句话说一遍。”

5. 原因和共同合作理由：

- 设计理由必须使用“可采用的共同理由是”等提案语气，不能把新增设定写成既定事实。
- “不要写死个人背景”时，禁止追加过去、梦境、家人、秘密或既往遭遇。
- 共同理由优先来自眼前公共处境、短期利益或当场选择。
- 安全回退使用短期务实合作，不要求共同过去。

6. 可朗读叙事和玩家能动性：

- 检测并拦截替玩家“伸手、后退、打开、猛地、意识到、感到、决定”等动作、感受或结论。
- DM 没说明调查对象时，prompt 要求只写“刚才检查的地方/目标”，不能擅自指定门、石板、箱子或道具。
- 失败调查安全回退保证不因一次失败卡团，并把选择权留给玩家。

7. 精确改写长度：

- 低于目标字数 80% 判定太短，高于 130% 判定太长。
- revision prompt 给明确上下限。
- 安全压缩支持目标字数，并在要求时保留编号步骤结构。

8. 重试策略：

- `read_aloud`、`spoken_line`、`dm_guidance`、`explanation`、`revision` 的交付质量失败都可触发一次定向重写。
- 正常成功仍为一次模型调用；不是所有请求都固定调用两次。

## 已做真实页面验收

在隔离验收团“综合规则验收团 · 0727-2101”中已经验证：

- 玩家拒绝去钟楼、要求三条主持方法且不要 NPC 台词/新线索/环境事件：模型失败后，安全回复正确变为三条主持方法，并尊重玩家选择。
- 店主发现玩家撒谎、只给一句克制台词：最终得到符合目的的单句台词。
- 共同合作理由且不要写死背景：最新回复用提案语气，不再追加玩家梦境或既往经历。
- “把刚才那段压到 80 字左右，保留三步结构”：最终约 70 多字并保留三步。

运行数据库 `model_runs` 的最近证据：正常成功调用约 2.8–3.9 秒；质量失败重写通常是两条约 2.8–3.3 秒的 reasoning run。页面观察到的更长时间还包含测试等待和渐显，不应全部算作模型 latency。

## 交接时未完成内容（本轮已处理）

交接列出的调查目标边界、玩家相识、交叉验收、运行副本同步、标准团恢复、门禁和记忆回写均已在本轮完成：

1. 未说明调查对象时，新增具体物件、替玩家动作/感受/认知或结论会被质量门拦截；二次候选仍不合格时使用不指定对象的事实受限回退。
2. 真实页面确认玩家相识请求返回三步主持方法，不写开场旁白、不编个人过去、不强推路线。
3. 交叉验收覆盖调查失败、玩家相识、NPC撒谎台词、共同理由、DM建议、精确长度与安全回退；无应用控制台错误。
4. 5 个功能文件已与运行副本逐一 `cmp` 一致；标准团已恢复到 `Scene 5 · 晨溪村庆功与升级`。
5. 完整门禁和 8787 健康检查通过；本文件和功能文件待本轮提交推送。

## 关键决策

- 不为 opener 单独建立一套僵硬规则；所有请求走同一个通用交付分类和质量门，只按交付用途选择契约。
- 场景事实只提供素材，不能覆盖 DM 当前请求的目的、格式、数量或限制。
- 允许可朗读叙事自由发挥非机械性的感官与文学细节，但所有内容仍是供 DM 采用的草案，不自动进入战役权威事实。
- 不替玩家决定动作、情绪、认知、背景、关系或下一步。
- DM 建议与世界内叙事严格分离；用户只问建议时不能偷偷推进世界状态。
- 模型候选优先保留，程序只做必要质量修复；只有明确违规或功能错位才重写/回退。
- 性能上保持“一次为常态、一次定向重写为上限”，不要恢复到每问固定多阶段、多次调用。
- 不接入外部 DeepSeek API；用户已经决定目前先不做 API 入口。
- 默认继续使用 `qwen3:30b-instruct`。Gemma4 已删除，不要重新下载或接回。

## 修改文件

当前未提交的功能文件：

- `backend/src/dnd_dm_assistant/integrations/ollama.py`
  - narrative temperature、context 和输出预算。
- `backend/tests/test_ollama.py`
  - narrative Ollama options 回归测试。
- `frontend/src/pages/GameTablePage.tsx`
  - 各 delivery mode 的语义契约、NPC 上下文收窄、重试与安全回退接线。
- `frontend/src/ui/gameTableAssistant.ts`
  - 类型推断、交付质量检查、安全回退、精确改写。
- `frontend/src/ui/gameTableAssistant.test.ts`
  - 新分类、边界、玩家能动性、理由、破冰、台词和长度回归。

本交接文件 `CODEX_HANDOFF.md` 会随本轮功能改动一并提交。

## 测试结果

### 当前改动已经通过

- 前端完整测试：37 个测试文件、150 项测试通过。
- 前端 TypeScript：通过。
- 前端 ESLint：通过。
- 前端 production build：通过。
- 后端定向 `tests/test_ollama.py`：6 项通过。
- 后端完整 `backend/tests`：245 项通过。
- Ruff：`backend/src backend/tests` 通过。
- mypy：本轮后端文件 `backend/src/dnd_dm_assistant/integrations/ollama.py` 通过。
- `git diff --check`：通过。
- `rg -n 'assistant-candidate-debug' frontend/src`：无匹配，未残留候选调试日志。

### 门禁命令注意事项

直接执行 `pytest -q backend/tests` 会因为 pytest 将 rootdir 识别为 `backend`，导致 `backend/tests/test_duskbell_maps.py` 无法导入仓库根的 `scripts`，报：

```text
ModuleNotFoundError: No module named 'scripts'
```

这不是本轮功能失败。当前可复现的正确命令是：

```bash
cd '/Users/inagi/codex/700-AI/local-dnd-dm-assistant'
source backend/.venv/bin/activate
PYTHONPATH=. pytest -q backend/tests
ruff check backend/src backend/tests
mypy backend/src/dnd_dm_assistant/integrations/ollama.py
```

不要为了本轮任务去修全项目既有的 mypy 历史问题；只检查本轮后端文件，或使用项目已经确认的严格门禁配置。

## 当前阻塞

没有已知代码级阻塞。当前工具沙盒对 loopback 的访问需要获准命令权限，但浏览器和获准的只读命令已确认服务正常。

Statsig 网络超时属于浏览器插件噪音，不是产品错误，不要因此修改应用。

## 下一步

1. 提交并推送本轮已验证的改动。
2. 后续继续扩展交付类型时，保持一次正常调用、一次定向重写上限和事实受限回退边界。
3. 继续使用运行副本数据库，避免把 D&D 与 COC 的服务和记忆混接。

## 不能破坏的现有行为

- 普通成功请求通常只调用一次模型；质量失败最多再定向重写一次。
- 游戏推进、执行和普通询问的意图边界不能混淆；“只问建议”不能推进 Scene、创建事件或更新玩家提示。
- 真正的 DM 推进、Scene 进入和右侧流程推进仍会创建公开安全的 `player_guidance`，玩家端随推进更新建议；不要影响这一同步链路。
- 中央对话生成时立即显示状态，并自动跟随最新渐显文字；完整模型结果先通过内容安全与结构校验后再渐显，不能直接展示未经验证的原始 token。
- 每个 campaign 保持独立、持久的副 DM 会话；刷新和切 Scene 后仍能承接上一条，但普通文案消息始终 `authoritative=false`。
- 被质量检查拦截的坏候选不能写入正式对话历史。
- 普通询问不能注入全团隐藏 NPC、隐藏目标、未发现线索、怪物名单或机关数据。
- 可朗读文案可以有文学性和自由发挥，但不能替玩家作决定，也不能将自由发挥自动写入战役事实。
- 建议、解释、台词、叙事、改写必须功能对位；不得用万能开场模板覆盖所有请求。
- 明确长度、数量、说话者、语气、玩家参与量和否定约束的优先级高于模型惯用模板。
- Scene 地图继续使用通用语义地图生成器；不能退回只为《暮铃磨坊》5 张地图特制或统一四房间模板。
- 图鉴、商店、装备/消耗品、玩家装备操作、商人/商店详情打开等既有功能不要因本轮副 DM 修改而回退。
- D&D 与 COC 使用独立环境变量、数据库和会话；共用 Ollama 权重不等于共享对话记忆，不能把两个项目的数据混接。
- 不重新安装/下载 Gemma4 或其他模型；优先复用现有 `qwen3:30b-instruct`、项目 venv 和缓存。
